# lsf-cluster-setup
I set up an IBM LSF cluster on RHEL 7/8 with one master node and multiple worker nodes. I configured job scheduling, queues, and resource policies, and handled troubleshooting for job execution and node issues.
